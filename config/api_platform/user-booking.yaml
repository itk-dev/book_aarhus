---
resources:
  App\Entity\Main\UserBooking:
    attributes:
      normalization_context:
        groups: ['userBooking']
    itemOperations:
      get: ~
      delete: ~
      patch: ~
    collectionOperations:
      get:
        openapi_context:
          operationId: get-v1-bookings
          description: Retrieves user bookings from a specific user.
          summary: Retrieves user bookings from a specific user.
          tags:
          parameters:
            - schema:
                type: string
                format: string
                example: "1"
              in: query
              name: userId
              required: true
              description: "ID of the user to retrieve bookings for"
          responses:
            '200':
              description: OK
              content:
                application/ld+json:
                  examples:
              headers: { }
      update_booking_time:
        input: App\Dto\UpdateBookingTimeInput
        method: POST
        path: '/user-bookings/update-booking-time'
        controller: App\Controller\UpdateBookingTimeController
        openapi_context:
          operationId: update_booking_time-v1-user-bookings
          description: Update the time for an existing booking.
          summary: Update the time for an existing booking.
          parameters:
          responses:
            '201':
              description: Time updated
              content:
                application/ld+json:
                  examples:
              headers: { }
