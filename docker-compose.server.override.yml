services:
  rabbit:
    image: rabbitmq:3.9-management
    networks:
      - app
      - frontend
    ports:
      - "15672"
    environment:
      - RABBITMQ_DEFAULT_USER="${RABBITMQ_USER}"
      - RABBITMQ_DEFAULT_PASS="${RABBITMQ_PASSWORD}"
      - RABBITMQ_ERLANG_COOKIE="asdfghjkl"